<button class="zr-btn zr-btn--ghost zr-btn--icon<%= typeof extraClass === 'string' && extraClass ? ' ' + extraClass : '' %>"
    type="button" data-action="theme" aria-label="Toggle theme">
    <svg class="zr-icon" aria-hidden="true"><use href="/icons.svg#i-moon"/></svg>
</button>
