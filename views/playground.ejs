<!DOCTYPE html>
<html lang="en" data-theme="<%= theme || 'light' %>">
<%- include('partials/shell/head-start', { pageTitle: 'Playground - Zettelrobbe' }) %>
    <script src="js/playground-analyzer.js" defer></script>
    <%- include('partials/shell/head-end') %>
    <%- include('partials/shell/body-open', {
    pageTitle: 'Playground',
    version: typeof version !== 'undefined' ? version : '',
        chatEnabled: typeof chatEnabled !== 'undefined' ? chatEnabled : false
    }) %>

        <!-- Main Content -->
            <div>
            <div>
                <div class="zr-alert zr-alert--warn" role="alert">
                    <svg class="zr-icon" aria-hidden="true"><use href="/icons.svg#i-alert"/></svg>
                    <div class="zr-alert__body">
                        <div class="zr-alert__title">The Playground is deprecated</div>
                        <p class="zr-sm">It has been removed from the navigation and will be removed entirely in a future release. Saved prompts and their ratings live in this browser only and are never stored on the server &mdash; copy anything you want to keep before then. To try a prompt against a single document, use <a href="/manual">Manual processing</a>.</p>
                    </div>
                </div>

                <!-- Analysis Section -->
                <div class="zr-module">
                    <h2 class="zr-module__title">Document Analysis</h2>
                    <div class="zr-col zr-col--loose">
                        <textarea 
                            id="analysisPrompt"
                            placeholder="Enter your analysis prompt here..."
                            class="zr-textarea"></textarea>
                        <div id="analysisActions" class="zr-row zr-row--wrap">
                                <button id="analyzeButton" class="zr-btn zr-btn--primary">
                                Analyze Documents
                            </button>
                        </div>
                    </div>
                </div>

                <div id="playgroundInitialLoading" class="zr-module" role="status" aria-live="polite" aria-busy="true">
                    <div class="zr-row">
                        <svg class="zr-icon zr-icon--spin" aria-hidden="true"><use href="/icons.svg#i-refresh"/></svg>
                        <div>
                            <div class="zr-strong">Loading document previews...</div>
                            <div id="playgroundInitialLoadingStatus" class="zr-sm zr-muted">Preparing thumbnail requests...</div>
                        </div>
                    </div>
                </div>

                <!-- Documents Grid -->
                <div id="documentsGridWrapper" class="playground-grid-wrap">
                    <div id="playgroundAnalysisOverlay" class="zr-overlay hidden" role="status" aria-live="polite" aria-busy="true">
                        <div class="zr-module">
                            <div class="zr-row">
                                <svg class="zr-icon zr-icon--spin" aria-hidden="true"><use href="/icons.svg#i-refresh"/></svg>
                                <div>
                                    <div id="playgroundAnalysisStatus" class="zr-strong">Analyzing documents...</div>
                                    <div class="zr-sm zr-muted">This can take a moment for larger batches.</div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div id="documentsGrid" class="card-grid"></div>
                </div>
            </div>
            </div>
    </div>

    <!-- Modal -->
    <div id="detailsModal" class="modal hidden">
        <div class="modal-overlay"></div>
        <div class="modal-container">
            <div class="modal-header">
                <h3 class="modal-title"></h3>
                <button class="modal-close">
                    <svg class="zr-icon" aria-hidden="true"><use href="/icons.svg#i-x"/></svg>
                </button>
            </div>
            <div class="modal-content">
                <div class="modal-loader hidden">
                    <svg class="zr-icon zr-icon--spin" aria-hidden="true"><use href="/icons.svg#i-refresh"/></svg>
                    Loading...
                </div>
                <div class="modal-data"></div>
            </div>
        </div>
    </div>
    <%- include('partials/shell/body-close') %>